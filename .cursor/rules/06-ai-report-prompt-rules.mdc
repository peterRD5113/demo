---
description: AI Report Prompt 自測報告規範 - 10條硬性規則 + 靜態審計方法論
alwaysApply: true
---

# AI Report Prompt 自測報告規範

> ⚠️ 提交時必須附上自測報告，缺少直接打回，不進入審查流程

## 使用方式

```
工具：codex-cli + gpt-5.3-codex-bak（推薦）或同等模型
步驟：
  1. 新開獨立 session（嚴禁用開發 session）
  2. 將題目 Prompt 填入 AI Report Prompt 的 {prompt} 位置
  3. 在指令末尾加入靜態審計聲明（避免 Docker 授權問題）：
     "不允許執行docker命令和執行測試，只讀靜態文件作報告"
  4. 讓模型按照 10 條硬性規則執行逐條驗收
  5. 輸出報告到 ./.tmp/report.md 後隨交付物提交
```

## AI Report Prompt 10 條硬性規則（模型必須遵守）

### Rule 1 - 逐點 Plan 推進
- 必須先調用 `update_plan` 一次性創建包含所有驗收大項的計劃清單
- 大項 1 設為 in_progress，其餘 pending
- 嚴格按計劃順序執行，完成後寫入 `./.tmp/**.md`

### Rule 2 - 不遺漏
- 每個大項下所有二級/三級條目必須覆蓋
- 若遇「不適用」也要明確標注「不適用」並說明原因與判定邊界

### Rule 3 - 可追溯證據
- 所有關鍵結論必須給出：**文件路徑 + 行號**
- 格式示例：`README.md:10`、`app/main.py:42`
- **禁止**僅憑推斷得出結論

### Rule 4 - 可運行優先
- 能運行就執行驗證；若受環境/權限限制無法運行：
  - 說明阻塞點是什麼
  - 給出本地可復現的完整命令
  - 基於靜態證據給出「可確認/不可確認」的邊界
- **沙盒環境限制導致的失敗（Docker/端口/網絡/只讀文件系統）不作為項目問題**

### Rule 5 - 不改代碼
- 純驗證任務，嚴禁修改核心代碼讓項目「看起來通過」
- 若確需修改，寫入「問題/建議」

### Rule 6 - 理論支撐
- 每個「通過/不通過」判斷必須說明依據與推理鏈路
- 標準：與手冊條款逐條對齊 / 常見工程實踐對齊 / 運行結果對齊

### Rule 7 - 支付 Mock 豁免
- 支付相關功能若採用 mock/stub/fake，在題目未要求真實聯調時不報問題
- 但必須說明：實現方式、啟用條件、是否存在誤上線風險

### Rule 8 - 安全問題優先（高於代碼風格）
重點核查：
- 認證入口（브루트 force 防護？）
- 路由級鑑權（未登錄是否能訪問受保護路由？）
- 對象級授權（IDOR：用戶A能否訪問用戶B的資源？）
- 功能級授權（普通用戶能否調用管理員接口？）
- 租戶/用戶數據隔離
- 管理/調試接口保護

### Rule 9 - 測試 + 日志必查
- 單元測試、API 接口功能測試、日志打印分類作為必填驗收項
- 必須說明：是否存在、是否可執行、覆蓋範圍、日志分類是否有敏感信息泄露風險

### Rule 10 - 測試覆蓋度靜態審計（必做，必須寫入報告）

#### 審計流程
```
① 抽取 Prompt 核心需求點 + 隱含約束（鑑權/越權/數據隔離/邊界條件/錯誤處理/幂等/分頁/並發/數據一致性）
   ↓
② 逐個定位測試文件與用例
   ↓
③ 建立映射：需求點 → 對應測試用例/斷言
   ↓
④ 對每個需求點給出覆盖判定（充分/基本覆蓋/不足/缺失/不適用/無法確認）
   ↓
⑤ 輸出完整《測試覆蓋度評估》章節
```

#### 必填：覆蓋映射表

| 需求點/風險點 | 對應測試用例(文件:行) | 關鍵斷言/fixture(文件:行) | 覆蓋判定 | 缺口 | 最小補測建議 |
|---|---|---|---|---|---|
| 用戶登錄 happy path | tests/test_auth.py:15 | assert resp.status==200:18 | 基本覆蓋 | 缺密碼錯誤場景 | 補充401測試 |

#### 必填：安全覆蓋審計（逐條）
```
① 認證（登錄/令牌/會話）：是否有測試？覆蓋判定？
② 路由鑑權（401場景）：是否有測試？
③ 對象級授權（IDOR：403場景）：是否有測試？複現思路？
④ 數據隔離（多用戶）：是否有測試？
```

#### 必填：總判定（只能選一個）
```
結論：通過 / 部分通過 / 不通過 / 無法確認
（必須說明：哪些關鍵風險被覆蓋、哪些未覆蓋會導致"測試通過但仍有嚴重缺陷"）
```

## 六大驗收維度（評分口徑）

| 維度 | 結論選項 |
|------|---------|
| 1. 硬性門槛 | 通過/部分通過/不通過/不適用/無法確認 |
| 2. 交付完整性 | 同上 |
| 3. 工程與架構質量 | 同上 |
| 4. 工程細節與專業度 | 同上 |
| 5. 需求理解深度 | 同上 |
| 6. 美觀度（前端/全棧） | 同上 |

## 問題分級標準

| 級別 | 含義 | 示例 |
|------|------|------|
| **阻塞** | 無法啟動/驗證 | docker-compose 缺 service 聲明 |
| **高** | 安全漏洞/核心功能缺失 | 無鑑權、IDOR漏洞 |
| **中** | 工程質量/測試不足 | 無錯誤處理、測試覆蓋率低 |
| **低** | 代碼風格/日志優化 | 無意義的 console.log |
