---
description: 核心評分標準與紅線規則 - 所有代碼任務必須遵守
alwaysApply: true
---

# 核心評分標準（Repo代码生成任务手册 V4.2.0）

你是一名在進行代碼考核任務的開發者，每個項目都必須通過以下標準。將這些標準貫穿到你生成的每一行代碼中。

## 🔴 一票否決紅線（違反任意一條直接失格）

### 可運行性
- 後端/全棧項目必須支援 `docker compose up` 一鍵啟動，無任何報錯
- 嚴禁依賴本地絕對路徑（`C:/Users/...`、`/home/user/...`）
- 嚴禁依賴宿主機未聲明的服務（所有服務必須在 docker-compose.yml 中聲明）
- 嚴禁引用需要鑑權的私有鏡像倉庫
- README 啟動步驟必須與代碼實際行為完全一致（端口/URL/賬號）

### 切題性
- 嚴格圍繞 Prompt 的業務目標開發，禁止擅自替換核心需求
- WebSocket 需求不得簡化為 HTTP 輪詢
- 核心功能缺失（如多輪對話→單次問答）直接失格

### 真實邏輯
- 登錄接口必須有真實校驗邏輯，禁止 `return "Login Success"`
- 查詢接口不得返回硬編碼數據，必須有真實查詢/處理
- 禁止以 Mock 欺騙核心業務邏輯（依賴外部昂貴API除外）

## 🟠 交付完整性要求

- 必須是完整工程項目（有目錄結構、配置文件）
- 禁止單文件交付（不能是一個幾千行的 main.py 或 index.html）
- 必須包含：`package.json` / `requirements.txt` / `pom.xml` 等

## 🟡 工程與架構質量

### 後端分層架構（必須）
```
Controller（路由/入口）
    ↓
Service（業務邏輯）
    ↓
Repository/DAO（數據操作）
```
**禁止**：在同一函數中同時進行數據庫操作+業務邏輯+API定義

### 前端組件規範
- 組件合理拆分，禁止數千行"上帝組件"
- 目錄語義化：`/components`, `/pages`, `/utils`, `/api`, `/hooks`

### 可維護性與擴展性（3.3.3）
- **禁止 Magic Number**：數字常量必須定義為有意義的常量名
  ```python
  # ✅ 正確
  MAX_LOGIN_ATTEMPTS = 5
  TOKEN_EXPIRE_HOURS = 24
  
  # ❌ 禁止
  if attempts > 5:  # 5 是什麼？
  token_expire = 24 * 3600
  ```
- **禁止深層嵌套 if-else**：超過 3 層嵌套必須重構（早返回/策略模式）
  ```python
  # ✅ 正確：早返回原則
  if not user: return error(404)
  if not user.is_active: return error(403)
  return process(user)
  
  # ❌ 禁止
  if user:
      if user.is_active:
          if user.has_permission:
              ...  # 深層嵌套
  ```
- 核心邏輯必須具備基本擴展空間，不能完全寫死（如：狀態機、策略模式、配置驅動）

### 錯誤處理標準
```json
// ✅ 正確格式
{"code": 400, "msg": "Invalid email format", "data": null}

// ❌ 禁止返回
Stack Trace / 500 原始錯誤 / 空響應
```

### 日志標準
- 關鍵業務流程（登錄、下單、數據變更）必須有日志
- 禁止無意義日志：`print("here")`, `console.log("111")`
- 日志必須包含必要上下文（時間、用戶ID、操作類型）
- **嚴禁**在日志中打印 token/密碼/密鑰

### 參數校驗（所有接口必須）
- Body/Query/Path 參數：判空、格式校驗、長度限制
- 使用 ORM 或參數化查詢，禁止字符串拼接 SQL
- 密碼不得明文存儲（必須 bcrypt/argon2 等加密）

## 🟢 美觀度（前端/全棧項目）3.6
- 必須使用主流 UI 框架：Ant Design / Material UI / Tailwind CSS / Bootstrap
- 布局對齊、間距統一，無溢出錯位、無內容錯位或亂碼
- 按鈕必須有 Loading/Disabled 狀態；鼠標懸停必須有樣式變化
- 接口失敗必須有 Toast 提示，禁止白屏或無反應
- 頁面不同功能區域必須有明確視覺區分（背景色/分隔線/留白/層級結構）
- **視覺一致性**：字體/字號/顏色/圖標樣式必須統一，禁止風格混雜
- **內容匹配**：圖片/插圖/裝飾元素必須與頁面實際內容一致，禁止不相關的佔位圖
- 頁面跳轉邏輯清晰，無死鏈，用戶能順暢完成核心業務操作

## API 返回規範
- 接口返回 JSON 必須結構清晰
- 列表數據必須分頁（page/pageSize/total）
- 禁止返回裸 JSON 堆砌

## 🔵 需求理解深度（3.5）─ 做完、做對、做好

### 3.5.1 識別隱含約束（必須主動思考，不能等 Prompt 明說）

| 場景 | 隱含約束 | 必須實現 |
|------|---------|---------|
| 電商系統 | 庫存扣減不能為負數 | 下單時校驗庫存，不足則拒絕 |
| 預定系統 | 同一時間段不能重複預定 | 創建預定時檢查時間衝突 |
| 訂單系統 | 前台顯示成功必須後台已存儲 | 數據庫持久化後再返回成功 |
| 用戶系統 | 刪除用戶不應影響歷史數據 | 使用邏輯刪除（soft delete） |
| 支付系統 | 重複支付不能重複扣款 | 冪等校驗（訂單狀態檢查） |

> **邏輯自洽原則**：數據流轉必須前後一致，不能出現「前台成功但後台未存儲」的情況。

### 3.5.2 拒絕機械式翻譯

- 代碼實現必須貼合 Prompt 設定的**用戶規模和使用場景**，不能生搬硬套通用模板
- 例如：Prompt 是「個人記帳 App」→ 不需要多租戶架構；「企業 SaaS」→ 必須考慮數據隔離
- 例如：Prompt 是「內部工具」→ 安全要求適中；「對外平台」→ 必須嚴格鑑權防攻擊
- 場景適配優先於技術炫技
