---
description: 項目類型判斷 + 容器化要求對照表 + 推薦技術棧版本
alwaysApply: true
---

# 項目類型分類與容器化要求

## 拿到題目後第一步：判斷項目類型

根據 Prompt 內容，先確定類型，再決定容器化策略：

| 項目類型 | 容器化要求 | 啟動方式 | 端口暴露 |
|---------|---------|---------|---------|
| **Web 前端（純前端）** | ❌ 不要求 Docker | 說明啟動命令即可 | 說明訪問端口 |
| **純後端** | ✅ **必須 Docker** | `docker compose up` | docker-compose.yml 顯式暴露 |
| **全棧** | ✅ **必須 Docker** | `docker compose up` | docker-compose.yml 顯式暴露 |
| **移動端 iOS / Android / 鴻蒙** | ❌ 不要求 | 說明構建/運行方式 | 說明調試方式 |
| **桌面端 Desktop / macOS** | ⚠️ 部分不要求 | 說明啟動方式 | 說明訪問方式 |
| **小程序** | ⚠️ 後端必須 Docker，前端不要求 | 後端：`docker compose up`；前端：微信開發者工具導入 | 前後端分別說明 |

## 小程序特殊說明
```
後端：docker 啟動
前端交付方式：
  1. 安裝微信開發者工具
  2. 交付經過混淆處理的代碼壓縮包
  3. 導入項目後，即使沒有 AppID，也可選擇「測試號」模式運行
```

## 壓縮包目錄命名規則
```
pure_frontend/   ← 純前端項目
pure_backend/    ← 純後端項目
fullstack/       ← 全棧項目
desktop/         ← 桌面端項目
mobile/          ← 移動端項目
miniprogram/     ← 小程序項目
```

## 廢棄（不可做）判斷標準

> 拿到題目後立刻判斷，符合任意一條直接按「廢棄/不可做」處理

```
❌ 廢棄條件：
① 核心功能必須調用第三方 SaaS 且無法 Mock（如：必須真實支付、必須SMS到達）
② 強制要求 Windows 系統才能運行（如：依賴 COM 組件、.NET Framework 特定版本）
③ Prompt 缺失核心主題（如："請做個網站" 無任何具體需求）
④ Prompt 缺失必要素材（如：需要特定圖片/素材但未提供）

✅ 可做（不需廢棄）：
- 支付功能 → 可用 Mock 實現（非真實扣款）
- 短信驗證碼 → 可返回固定驗證碼或 Console 輸出
- AI 大模型調用 → 可 Mock 返回固定結果
- 地圖 API → 可用靜態坐標/Mock 數據
```

## 推薦技術棧版本（Prompt 未指定時使用）

### 前端
| 技術 | 推薦版本 |
|------|---------|
| Node.js | 18.x LTS（≥18.16.0） |
| npm | 9.x |
| pnpm | 8.x |
| React | 18.x |
| Vue | 3.x |
| TypeScript | 5.x |

### 後端
| 技術 | 推薦版本 |
|------|---------|
| Node.js | 18.x LTS |
| Python | 3.10.x |
| Java | 17.x LTS |
| FastAPI | 0.100+ |
| Spring Boot | 3.x |

### 數據庫 & 外部服務
| 技術 | 推薦版本 |
|------|---------|
| MySQL | ≥ 8.0.x |
| PostgreSQL | ≥ 14 |
| SQLite | ≥ 3.39 |
| Redis | 7.x |
| Chrome（測試用）| ≥ 120 |
